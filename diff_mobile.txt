--- Checking wayspot_rehber_mobil_part1.md (11 files) ---

MISMATCH: WaySpotMobile/src/utils/constants.js
--- expected
+++ actual
@@ -1,6 +1,3 @@
-export const API_BASE_URL = 'http://10.0.2.2:5000/api'; // Android emulatore icin localhost
-// Gercek cihaz icin: 'http://BILGISAYAR_IP:5000/api'
-// iOS simulatore icin: 'http://localhost:5000/api'
-
+export const API_BASE_URL = 'http://10.0.2.2:5075/api';
 export const COLORS = {
   primary: '#2563EB',

MISMATCH: WaySpotMobile/src/api/client.js
--- expected
+++ actual
@@ -11,5 +11,4 @@
 });
 
-// Request interceptor - Token ekleme
 client.interceptors.request.use(
   async (config) => {
@@ -23,5 +22,4 @@
 );
 
-// Response interceptor - 401 handling
 client.interceptors.response.use(
   (response) => response,

MISMATCH: WaySpotMobile/src/screens/auth/RegisterScreen.js
--- expected
+++ actual
@@ -10,5 +10,5 @@
   const [email, setEmail] = useState('');
   const [password, setPassword] = useState('');
-  const [role, setRole] = useState(1); // 1=User, 2=Business
+  const [role, setRole] = useState(1);
   const [loading, setLoading] = useState(false);
   const { register } = useAuth();

MISMATCH: WaySpotMobile/src/navigation/MainNavigator.js
--- expected
+++ actual
@@ -15,5 +15,4 @@
   const { user } = useAuth();
   const isBusiness = user?.role === 2;
-  const isAdmin = user?.role === 3;
 
   return (
--- Checking wayspot_rehber_mobil_part2.md (9 files) ---

MISMATCH: WaySpotMobile/src/screens/discover/DiscoverScreen.js
--- expected
+++ actual
@@ -1,3 +1,3 @@
-import React, { useState, useCallback } from 'react';
+import React, { useState, useCallback, useEffect } from 'react';
 import {
   View, FlatList, RefreshControl, StyleSheet, Text, Alert,
@@ -44,5 +44,5 @@
   }, []);
 
-  useState(() => {
+  useEffect(() => {
     fetchDiscover();
   }, []);

MISMATCH: WaySpotMobile/src/screens/business/BusinessDetailScreen.js
--- expected
+++ actual
@@ -7,4 +7,5 @@
 import client from '../../api/client';
 import LoadingSpinner from '../../components/LoadingSpinner';
+import ReviewCard from '../../components/ReviewCard';
 import { COLORS } from '../../utils/constants';
 
@@ -44,5 +45,4 @@
   return (
     <ScrollView style={styles.container}>
-      {/* Harita */}
       <MapView
         provider={PROVIDER_GOOGLE}
@@ -61,5 +61,4 @@
       </MapView>
 
-      {/* Bilgiler */}
       <View style={styles.infoCard}>
         <Text style={styles.name}>{business.name}</Text>
@@ -88,5 +87,4 @@
       </View>
 
-      {/* Yorumlar */}
       <View style={styles.reviewsSection}>
         <Text style={styles.sectionTitle}>Yorumlar</Text>
@@ -95,17 +93,5 @@
         ) : (
           reviews.map((review) => (
-            <View key={review.id} style={styles.reviewCard}>
-              <View style={styles.reviewHeader}>
-                <Text style={styles.reviewUser}>{review.username}</Text>
-                <View style={styles.ratingBadge}>
-                  <Icon name="star" size={14} color={COLORS.warning} />
-                  <Text style={styles.ratingText}>{review.rating}</Text>
-                </View>
-              </View>
-              <Text style={styles.reviewComment}>{review.comment}</Text>
-              <Text style={styles.reviewDate}>
-                {new Date(review.createdAt).toLocaleDateString('tr-TR')}
-              </Text>
-            </View>
+            <ReviewCard key={review.id} review={review} />
           ))
         )}
@@ -132,10 +118,3 @@
   sectionTitle: { fontSize: 18, fontWeight: 'bold', color: COLORS.dark, marginBottom: 12 },
   noReviews: { color: '#9CA3AF', fontSize: 14, textAlign: 'center', marginTop: 20 },
-  reviewCard: { backgroundColor: COLORS.white, padding: 16, borderRadius: 12, marginBottom: 10, elevation: 1 },
-  reviewHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 },
-  reviewUser: { fontSize: 14, fontWeight: '600', color: COLORS.dark },
-  ratingBadge: { flexDirection: 'row', alignItems: 'center', backgroundColor: '#FEF3C7', paddingHorizontal: 8, paddingVertical: 2, borderRadius: 12 },
-  ratingText: { fontSize: 12, fontWeight: '600', color: '#92400E', marginLeft: 4 },
-  reviewComment: { fontSize: 14, color: '#4B5563', lineHeight: 20, marginBottom: 6 },
-  reviewDate: { fontSize: 12, color: '#9CA3AF' },
 });
MISMATCH: WaySpotMobile/src/screens/review/AddReviewScreen.js
--- expected
+++ actual
@@ -43,7 +43,5 @@
     setLoading(true);
     try {
-      // NOT: Gercek uygulamada fotografi once sunucuya yukleyip URL almaniz gerekir.
-      // Simdilik placeholder URL kullaniyoruz. Backend'e image upload endpoint'i eklenmeli.
-      const photoUrl = photoUri; // Gecici: gercekte upload sonrasi URL olmali
+      const photoUrl = photoUri;
 
       await client.post(`/businesses/${businessId}/reviews`, {
@@ -69,5 +67,4 @@
       <Text style={styles.subtitle}>Yorumunuzu paylasin</Text>
 
-      {/* Rating */}
       <View style={styles.ratingContainer}>
         {[1, 2, 3, 4, 5].map((star) => (
@@ -82,5 +79,4 @@
       </View>
 
-      {/* Comment */}
       <TextInput
         style={styles.commentInput}
@@ -93,5 +89,4 @@
       />
 
-      {/* Photo */}
       <View style={styles.photoSection}>
         {photoUri ? (

MISMATCH: WaySpotMobile/src/screens/business/BusinessDashboardScreen.js
--- expected
+++ actual
@@ -32,5 +32,4 @@
       <Text style={styles.header}>Isletme Paneli</Text>
 
-      {/* Kartlar */}
       <View style={styles.cardsRow}>
         <View style={[styles.card, { backgroundColor: '#EFF6FF' }]}>
@@ -59,5 +58,4 @@
       </View>
 
-      {/* Son Yorumlar */}
       <Text style={styles.sectionTitle}>Son Yorumlar</Text>
       {stats.recentReviews.length === 0 ? (

Check finished.
